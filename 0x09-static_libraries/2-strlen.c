#include "main.h"
/**
 *   _strlen - program returns length of a string
 *     @s: input value
 *       Return: length
 */
int _strlen(char *s)
{
int longa = 0;

while (*s != '\0')
{
longa++;
s++;
}
return (0);
}
